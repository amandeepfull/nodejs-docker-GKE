version: '3'
services: 
  
  redis-server:
    image: 'redis'

  contact-app:
    restart: always
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports:
      -  "3032:3032" 
    volumes:
      - /contact-app/node_modules
      - .:/contact-app
